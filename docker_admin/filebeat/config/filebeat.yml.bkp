filebeat.prospectors:
- input_type: log
  paths:
     - logs/sms/*.csv
#  fields: {log_type: sms}
#  paths:	 
#     - logs/voip/*.csv
#  fields: {log_type: voip}   
#  document_type: voip
  fields_under_root: true

output.logstash:
  enabled: true
  hosts: ["logstash:5044"]
  
logging.to_files: true
logging.files:
